

---
import AttachIcon from "../Icons/AttachIcon.astro"; 

const { title, description, tags, srcImage, href } = Astro.props;

---
<article>
  <img class="w-full rounded-xl aspect-video" src={srcImage} alt="Project" />
  <div class="py-4" >
    <a href={href} target="_blank"   class="hover:underline font-bold text-xl mb-2 flex items-center gap-2">{title} <AttachIcon /> </a>
    <p class="text-color-card text-base">
      {description}
    </p>
  </div>
  <!-- <div class="pb-2 flex gap-1">
    {
      tags.map((tag) => {        
        return(
        <span class={`${tag.class} flex items-center gap-x-1 rounded-full text-[12px] py-1 px-1 border border-white/10`}>
          <tag.icon />
          {tag.name}
        </span>
      )
      })
    }
  </div> -->
</article>
